language: node_js

node_js:
  - "8"

env:
  - CXX=g++-4.8

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - gcc-4.8
    - g++-4.8

install:
  - npm install -g ethereumjs-testrpc
  - npm install -g truffle
  - npm install bluebird
  - npm install chai
  - npm install

script:
  - testrpc -l 10000000 & truffle test
